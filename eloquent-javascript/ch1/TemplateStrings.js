const obj = {
"name" : "anshul",
"roll" : 123
}

console.log("directly printing object"+obj) // this wont show values..plus operator
console.log("directly printing object",obj) // will work fine

// template str

console.log(`candidate name is ${obj.name} and his roll no is ${obj.roll}`) // printing btw strings example
